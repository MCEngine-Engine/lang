enable: false
licenses:
  license: free
  secret_key:

database:
  type: sqlite  # sqlite | mysql | postgresql

  sqlite:
    path: lang.db

  mysql:
    host: localhost
    port: "3306"
    name: mcengine_lang
    user: root
    password: ""

  postgresql:
    host: localhost
    port: "5432"
    name: mcengine_lang
    user: postgres
    password: ""

github:
  token:
